(() => {
  const SUPABASE_URL='https://wmeuebjbvoqudhpwtxyn.supabase.co';
  const SUPABASE_KEY='sb_publishable_jYrKnt_Unuv5M6XT1t0AaQ_quQTOpCD';
  const supabase=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY);
  const root=document.querySelector('#dashboard-view');if(!root)return;
  const section=document.createElement('section');section.className='campaign-panel';section.innerHTML='<div class="campaign-header"><div><p class="eyebrow">Players</p><h2 data-dir="title">Player directory</h2><p class="muted" data-dir="subtitle">Find players by username to invite them to a campaign.</p></div></div><div class="campaign-invite"><input id="player-search" type="search" maxlength="30" autocomplete="off" placeholder="Search username…"><button id="player-search-button" class="button button-primary" type="button" data-dir="search">Search</button></div><div id="player-results" class="campaign-grid"></div>';
  root.appendChild(section);
  const tr={en:{title:'Player directory',subtitle:'Find players by username to invite them to a campaign.',search:'Search',empty:'Search for a username.',none:'No player found.',error:'Unable to search players.'},fr:{title:'Annuaire des joueurs',subtitle:'Recherchez un joueur par son pseudo pour l’inviter dans une campagne.',search:'Rechercher',empty:'Recherchez un pseudo.',none:'Aucun joueur trouvé.',error:'Impossible de rechercher les joueurs.'}};
  const lang=()=>localStorage.getItem('preferredLanguage')||'en';const t=k=>tr[lang()][k]||k;const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  async function search(){const q=section.querySelector('#player-search').value.trim();const out=section.querySelector('#player-results');if(!q){out.innerHTML=`<div class="campaign-card"><p class="muted">${t('empty')}</p></div>`;return}const {data,error}=await supabase.from('profiles').select('id,username,display_name').ilike('username',`%${q}%`).order('username').limit(20);if(error){out.innerHTML=`<div class="campaign-card"><p>${esc(t('error'))}</p></div>`;return}if(!data?.length){out.innerHTML=`<div class="campaign-card"><p class="muted">${t('none')}</p></div>`;return}out.innerHTML=data.map(p=>`<article class="campaign-card"><h3>${esc(p.username)}</h3>${p.display_name&&p.display_name!==p.username?`<p class="campaign-meta">${esc(p.display_name)}</p>`:''}</article>`).join('')}
  section.querySelector('#player-search-button').addEventListener('click',search);section.querySelector('#player-search').addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();search()}});window.addEventListener('language-changed',()=>{section.querySelectorAll('[data-dir]').forEach(el=>el.textContent=t(el.dataset.dir));search()});
})();
